package examples.java.receiver.kotlinfauxproperty

class Design0 {
    private var counter = 0
    fun getCounter() = counter
    fun setCounter(setCounter: Int) {
        counter = setCounter
    }
}